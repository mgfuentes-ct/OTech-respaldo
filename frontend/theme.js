// frontend/theme.js
// Theme switching is disabled. The system uses a clean light theme by default.
document.addEventListener('DOMContentLoaded', () => {
    // Ensure the light-theme class is NOT present if we moved everything to :root
    // Or we can just leave it empty.
});
